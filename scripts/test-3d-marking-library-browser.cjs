const {chromium}=require('playwright');
const base=process.env.GCTS_TEST_URL??'http://127.0.0.1:8893';
const assert=require('node:assert/strict');
(async()=>{const browser=await chromium.launch({headless:true,...(process.env.CHROME_PATH?{executablePath:process.env.CHROME_PATH}:{})});const context=await browser.newContext({viewport:{width:1440,height:1050}});const page=await context.newPage();const errors=[];page.on('pageerror',e=>errors.push(e.message));
await page.goto(base+'/3d-lattice-tiler/');await page.waitForFunction(()=>document.querySelector('#status').textContent.startsWith('Ready'));
await page.selectOption('#tile','cube');await page.waitForFunction(()=>document.querySelector('#status').textContent.startsWith('Ready'));
assert.ok(await page.locator('.marking-use').isDisabled());await page.click('.marking-new');await page.waitForFunction(()=>document.querySelector('#status').textContent==='Marked window verified.');
assert.ok(await page.locator('#markingLearning').isHidden());assert.ok(await page.locator('.viewer').isVisible());assert.equal(await page.locator('#markingLibrary select option').count(),1);assert.match(await page.locator('#markingLibrary select').textContent(),/points.*values/);
await page.click('.marking-use');await page.waitForFunction(()=>document.querySelector('#status').textContent==='Marked window verified.');assert.match(await page.locator('#inspection').textContent(),/Reused browser marking/);
await page.reload();await page.waitForFunction(()=>document.querySelector('#status').textContent.startsWith('Ready'));await page.selectOption('#tile','cube');await page.waitForFunction(()=>document.querySelector('#status').textContent.startsWith('Ready'));assert.ok(await page.locator('.marking-use').isEnabled());
await page.check('#mirrors');await page.waitForFunction(()=>document.querySelector('#status').textContent.startsWith('Ready'));assert.ok(await page.locator('.marking-use').isDisabled());await page.uncheck('#mirrors');await page.waitForFunction(()=>document.querySelector('#status').textContent.startsWith('Ready'));await page.click('.marking-new');await page.waitForFunction(()=>document.querySelector('#status').textContent==='Marked window verified.');assert.equal(await page.locator('#markingLibrary select option').count(),2);assert.equal(new Set(await page.locator('#markingLibrary select option').allTextContents()).size,2);
if(process.env.GCTS_SCREENSHOTS)await page.screenshot({path:process.env.GCTS_SCREENSHOTS+'/reuse-v2.png'});
console.log('V2: cold learn, auto-switch, reuse, reload, domain filtering, distinct runs pass',errors);
await page.goto(base+'/apps/3d-lattice-tiler/legacy.html?tile=cube');await page.waitForFunction(()=>document.querySelector('#markingLibrary .marking-new')&&!document.querySelector('#markingLibrary .marking-new').disabled);await page.fill('#maxTilesInput','8');await page.click('.marking-new');await page.waitForFunction(()=>!document.querySelector('.marking-use').disabled,{},{timeout:30000});
assert.ok(await page.locator('#markingLearning').isHidden());await page.click('.marking-use');await page.waitForFunction(()=>!document.querySelector('.marking-use').disabled,{},{timeout:30000});console.log('V1 status:',await page.locator('#status').textContent());if(process.env.GCTS_SCREENSHOTS)await page.screenshot({path:process.env.GCTS_SCREENSHOTS+'/reuse-v1.png'});
assert.deepEqual(errors,[]);await browser.close();console.log('PASS both live browser interfaces, no page errors');})().catch(e=>{console.error(e);process.exit(1)});
